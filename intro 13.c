#include <stdio.h>
#include <conio.h>
void main()
{
	int a,b;
	printf("\nenter two numbers:");
	scanf("%d%d",&a,&b);
	if(a>b)
	{
	
	   
	   printf("\n%d is larger than %d",a,b);
    }
	
	  else
	  {
	  
	   printf("\n%d is larger than%d",b,a);
}
}
